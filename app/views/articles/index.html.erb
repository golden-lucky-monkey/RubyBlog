<h1 class="text-center mt-5" style="font-size: 50px;">Articles</h1>

<p class="text-center mt-2" style="font-size: 30px;">Our blog has <%= Article.public_count %> articles and counting!</p>

<ul>
    <% @articles.each do |article| %>
        <% unless article.archived? %>
            <li>
                <%= link_to article.title, article %>
            </li>
        <% end %>
    <% end %>
</ul>

<%= link_to "New Article", new_article_path %>